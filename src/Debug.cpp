#include "Debug.h"

// Debug 
const int startupProcedure = 1; // 1- Startup procedure on, 0 - Startup procedure off

//Temporary settings for now until proper settings are fully implemented
int currentClockStyle = 1; //0 is first, indexes in an array  //0-7s //1-7sg //2-cs
int drawBackground = 1; // 1 - Background draws, 0 - background doesn't draw
int drawDate = 1; // 1 - Date draws, 0 - Date doesn't draw
int militaryTime = 0; // 1 - 24 Hour, 0 - 12 Hour
